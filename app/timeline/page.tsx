'use client'

import { Layout } from '@/components/Layout/Layout'
import { MarketResponseTimeline } from '@/components/Timeline/MarketResponseTimeline'

export default function TimelinePage() {
  return (
    <Layout>
      <MarketResponseTimeline />
    </Layout>
  )
}